services:
  - type: web
    name: synthia-server
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python server.py
    healthCheckPath: /health
    envVars:
      - key: PORT
        value: 10000
      - key: GROQ_KEY
        sync: false
